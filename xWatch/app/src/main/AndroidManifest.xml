<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.redsun.xwatch"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="org.redsun.xwatch.LoadingActivity"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.redsun.xwatch.MainActivity"
            android:label="@string/main_label"
            android:launchMode="singleInstance" >
        </activity>

        <activity
            android:name="org.redsun.xwatch.CameraActivity"
            android:label="@string/camera_label"
            android:screenOrientation="portrait"
            android:launchMode="singleInstance" >
        </activity>

        <activity
            android:name="org.redsun.xwatch.EventListActivity"
            android:label="@string/eventList_label"
            android:launchMode="singleInstance" >
        </activity>

        <service
            android:name="org.redsun.xwatch.EventService"
            android:enabled="true" >
            <intent-filter>
                <action android:name="com.zcy.android.WATCH" />
            </intent-filter>
        </service>

        <receiver android:name="org.redsun.xwatch.BootReceiver" >
            <intent-filter>

                <!-- 系统启动完成后会调用 -->
                <action android:name="android.intent.action.BOOT_COMPLETED" >
                </action>
            </intent-filter>
        </receiver>
    </application>

</manifest>